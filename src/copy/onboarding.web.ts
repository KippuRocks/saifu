// Onboarding copy on Saifu Web (T-030-18): a browser without passkeys, which
// includes any page not served over https.

export const ONBOARDING_COPY = {
  passkeysUnavailable: "This browser cannot create passkeys, so Saifu cannot be set up in it.",
} as const;
