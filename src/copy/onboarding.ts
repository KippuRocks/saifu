// Onboarding copy that depends on where Saifu runs (T-030-04, T-030-18).

export const ONBOARDING_COPY = {
  passkeysUnavailable: "This phone cannot create passkeys, so Saifu cannot be set up on it.",
} as const;
